const Loader = () => {
  return (
    <div className="fixed top-0 left-0 w-full bg-black/50 z-50 flex items-center justify-center h-full">
      <div className="w-24 h-24 border-[10px] border-blue-600 border-t-transparent rounded-full animate-spin"></div>
    </div>
  );
};

export default Loader;
